#include <stdio.h>
#include <stdlib.h>
#include "string_pool.h"
#include "graph.h"
void cli();

int
main(int argc, char**argv)
{
	struct vertext_node *header=NULL;
	struct vertext_node *current=NULL;
	cli();
	exit(0);
}
